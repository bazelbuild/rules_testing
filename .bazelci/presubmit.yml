buildifier: latest
validate_config: 1
matrix:
  platform: ["ubuntu2004", "windows", "macos"]
  bazel: ["latest", "5.x"]
tasks:
  all_tests:
    #name: ${{ format('Bazel v{0}', bazel) }}
    #name: "${{bazel}}"
    #name: ${{'my name'}}
    # works name: ${{bazel}}
    # works name: "My name"
    # Doesn't work name: Bazel ${{bazel}}
    name: "Bazel ${{bazel}}"
    platform: ${{platform}}
    bazel: ${{bazel}}
    test_targets:
      - "..."
